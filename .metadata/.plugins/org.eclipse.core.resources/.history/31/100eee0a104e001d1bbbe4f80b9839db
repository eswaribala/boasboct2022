package com.boa.customerapi.services;

import java.util.List;

import com.boa.customerapi.models.Address;

public interface AddressService {

	//Address CRUD
	
			Address addAddress(Address Address);
			List<Address> getAllAddresss();
			Address updateAddress(long customerId, long mobilNo, String email,String password);
			boolean deleteAddressById(long customerId);
			Address getAllAddressById(long customerId);
}
